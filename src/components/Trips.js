import { React } from "react";
import { SafeAreaView, FlatList, StyleSheet, Text, View } from "react-native";

const Trip = () => {
  return (
    <SafeAreaView>
      <FlatList>
        <Text>Hello trips</Text>
      </FlatList>
    </SafeAreaView>
  );
};
export default Trip;
